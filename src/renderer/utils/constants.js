/**
 * Renderer constants for the UI layer.
 * @module renderer/constants
 */

/** @type {number} Default page size for paginated views */
export const DEFAULT_PAGE_SIZE = 10;

/** @type {number} Duration in milliseconds before toast messages are removed */
export const TOAST_DURATION_MS = 5000;
